//what ever the extension(type of data) of the file might be .txt or .js both are called as source files only.
//the difference between .js and .txt is that which file we have to inject into to a software and execute
//there are  some softwares(compilers or interpretes) will have the capability of understanding the syntax
console.log("HEllo World")// Save the file and run
/*Now set the Git environment:
1.login
2.create repository
3.create codespace
4.got to view->command pallet(ctrl+shift+P)*/